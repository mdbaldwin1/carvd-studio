# Full-review round 3 remediation — K1–K8

Status: all K1–K8 fixed; complete desktop/static gates PASS, unreleased.
Starting candidate: `87a564ce3fefe19e821c4db1f176ae4bd3c66a9f`.
Qualified code: `a6ce3c583a074f9a5bb91820c4568fe8e067d603`
(`fix: preserve custom cut material and fabrication lifecycle`).
Environment: 2026-09-09, macOS arm64, Node 23.10.0, branch
`codex/custom-cuts-release`, worktree `.worktrees/custom-cuts-release`.
Desktop remains 1.3.0. No subagent or additional reviewer was used.

## Independent reproduction and focused evidence

Raw logs and generated PDF artifacts: `/tmp/carvd-review-round6.HWV4fh`.
Initial independent reproductions total **30 failed / 2 controls passed**:
`initial-red.log` 18 failed/2 passed, `fabrication-red.log` 7 failed,
`lifecycle-red.log` 5 failed. Every K1–K8 finding was reproduced before its fix.
All numeric geometry tests use real Three.js, literal rays, measured volume,
or independent cell integration, not mocked dimensions or permissive bounds.

| Finding                                                    | RED and root cause                                                                                                                                                                                    | Fix / production files                                                                                                                                                                                                                                                                                                                                                         | Focused GREEN and adjacent coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| K1 Important: round/rounded openings collide as blank wood | Initial two opening probes fail; expanded all-face/XYZ/contact matrix **32 failures**. `overlapPolicy` has only rectangular contour handling and falls back to the blank OBB for round families.      | `overlapPolicy.ts` dispatches supported circular/rounded parts to `partsOverlapInMaterial` in `partFeatureGeometry.ts`. BVH surface samples and transverse triangle intersections prove points strictly inside both rendered solids; winding crossings cancel opposing internal caps. Geometry is cloned before BVH construction/transforms, preserving the render cache.      | **37/37**, `k1-final.log`: six circular faces, Top/Bottom rounded slots at 0/37 degrees, XYZ 0/0/0, 30/20/40 and 90/90/0, nonzero translations, both argument orders, literal facet contact, 0.0001-inch intrusion, blind floors, 16/128-hole compound grids, identical copies, and the exact four-family overlapping-copy stall fixture. Final cold timings: copied four-family solids **39.4ms**, 16 holes **25.1ms**, 128 holes **103.9ms**, each under a 1,000ms numeric-test budget. |
| K2 Important: opposing tenons restore material             | Length 6/8 gives incorrect 16/28 in³; the length-5 control is 10. One outline assumes shoulders never cross.                                                                                          | `partFeatureGeometry.ts` replaces the one-tongue outline builder with per-depth-band removal rectangles for every tenon, unioned with all other removals.                                                                                                                                                                                                                      | K2/K3 expanded **8 RED failures / 2 controls -> 10/10 GREEN**, `tenons-expanded-red.log`, `tenons-green.log`. Lengths 5/6/8 all retain exactly 10 in³, shoulder rays stay empty, and tongue top stays Y=.25.                                                                                                                                                                                                                                                                              |
| K3 Important: later same-end tenon ignored                 | `.find` selects only the first tenon on an end; A/B renders 35 versus B/A 31 in³.                                                                                                                     | Same all-tenon subtraction path as K2. Same-end operations remain accepted when their successive removals leave material; they do not act as additive independent tongues.                                                                                                                                                                                                     | Both A/B orders retain 31 in³. Five independent cell-integrated fixtures cover unequal lengths/widths/thicknesses/offsets, both ends, disjoint retained tongues, three operations and both list orders. No weak volume assertion or clamping was introduced.                                                                                                                                                                                                                              |
| K4 Important: glue-up panel cuts copied to each strip      | Four RED tests: strip data, both actual PDFs, CSV repeat the centered handhold three times. New real UI test also RED.                                                                                | `types.ts`, `cutListOptimizer.ts`, `cutListInstructions.ts`, `pdfExport.ts`, `CutListPartsTab.tsx`: strips contain no panel features. Optional separate post-glue-up records retain original panel identity/finished size/features and explicit stage. They do not participate in packing or blank counts. Stage affects grouping.                                             | **5/5** K4 tests: three 20x4 strips, one 20x12 panel operation record, all packed placements are strips, one handhold in each PDF/CSV and the real UI, identified post-glue-up section. Six actual PDF artifacts generated with K8; changed pages visually inspected.                                                                                                                                                                                                                     |
| K5 Important: Cut List ignores invalid dowel relationships | Diameter mismatch independently fails; expanded diameter/depth/alignment/missing/interference **5 RED failures**. Per-hole validation is insufficient for a pair.                                     | `dowelJointUtils.ts` exposes attributed relationship issues while preserving the original string API. `projectStore.ts` runs the shared relationship validator once at fabrication boundary, deduplicates exact issues, includes involved part names and actionable corrections.                                                                                               | **5/5**, `k5-green.log`: valid pair passes; each distinct invalid relationship produces one attributed blocking fabrication error. Existing pair-authoring/save/copy/assembly suites remain included in full gates.                                                                                                                                                                                                                                                                       |
| K6 Important: min/max references lost on edit              | Twelve matrix tests RED: editor reconstruction hardcodes center even when the source stores min/max.                                                                                                  | `partFeatureEditorState.ts` deep-copies optional reference data into circular/rounded drafts and back; absent draft data retains the old center default.                                                                                                                                                                                                                       | **34 utility tests + 3 real workspace Save Cut tests**. Both coordinates min/center/max, all six circular faces, all pattern kinds, label-only edit, actual file parse/deserialize, mirror/re-edit, literal rounded geometry/rays on both supported faces, and legacy omitted secondary references. Unsupported rounded legacy faces retain metadata but do not become newly supported.                                                                                                   |
| K7 Important: global Save/Exit discards active panel edits | Five actual workspace/hook/store RED tests; then three additional file-hook RED cases expose shortcut/save routing and focused inputs. Shared session dirtiness never sees local React draft changes. | `PartCutsWorkspace.tsx`, `partCutsEditingStore.ts`, `usePartCutsEditing.ts`, `useFileOperations.tsx`, `App.tsx`: register transient inspector commit/dirty state, synchronously flush focused fraction input before Save/Exit, read fresh committed session features, validate exact errors, and save part then file. Native Close Project routes through the same exit guard. | **11 workspace/store tests**, all **93 file-hook tests**, six new actual Electron route tests **6/6 (9.0s)**. Header/Cmd+S/native Save each cover label, still-focused .755, invalid 20-inch hole and actual file contents; Exit/logo/native Close each cover local label/.74, Keep Editing and Discard. Explicit Save Cut undo/redo remains session-local; final part update is one project undo step.                                                                                   |
| K8 in-scope pre-existing blank precision                   | Three RED tests: real Cut List/Project Report PDFs and CSV round 10.005x2.74x.74.                                                                                                                     | `pdfExport.ts` and `CutListPartsTab.tsx` reuse the exact fabrication measurement formatter. `fitBlankDimensionColumns` uses real text metrics for numeric columns, keeping stock/operations fixed and abbreviating table names using the existing policy.                                                                                                                      | **5/5** precision cases within the nine fabrication tests: imperial and metric actual PDFs, CSV, distinct grouped sizes, and exact fractions. Visual inspection found crowded metric thickness; two real-PDF position/font-width assertions reproduced RED, then pass with at least 4pt separation.                                                                                                                                                                                       |

New regressions live in `customCutsRound6.test.ts`,
`customCutsRound6Fabrication.test.ts`, `customCutsRound6Lifecycle.test.tsx`,
`customCutsRound6Panel.test.tsx`, `useFileOperations.test.tsx`, and
`tests/e2e/custom-cuts-round6.spec.ts`. No source `.only` or `.skip` was added.

## Aggregate reconciliation and adjacent self-audit

The first broad renderer run was 4,453 passing / one failure because the old
hook assertion expected the intentionally replaced vague error. It now checks
the exact hole-profile error. No production validation was weakened.

The first full run passed 4,460 renderer and 213 main tests, then **140/141
Electron**. Opening Part Cuts immediately after copying a mitre/dado/blind-hole/
rounded-slot part stalled. Two unchanged isolated reproductions and temporary
instrumentation traced it to collision-time CSG before editor mount, not a
React lifecycle loop or host flake. That attempted collision implementation
was removed. Finite BVH queries retain the original physical oracle matrix;
the exact copy fixture is now a numeric/performance regression. The unchanged
real copy/paste/edit flow then passed **1/1 in 3.4s** (`copy-green.log`). All
temporary diagnostic statements were removed. Neither timeout nor assertion
was relaxed, and the existing Electron fixture was not changed.

The following renderer rerun found 29 PDF mock failures: the older minimal
jsPDF mock lacked `getFontSize`, now required by measured numeric columns.
The mock gained that real API method; no PDF assertion was weakened. Actual
jsPDF output tests already passed. The final focused four-file run passes
**107/107**, including the exact overlapping-copy fixture.

The circular/rounded collision self-audit covers empty openings, literal
polygonal walls, tangency versus intrusion, all supported faces, composed
rotations, blind floors, both argument orders, unchanged borrowed geometry,
large patterns, coincident copies and overlapping mixed-family copies.
Earlier geometry clipping still uses its bounded exact stock halfspaces;
collision-time Boolean construction is no longer used.

The tenon audit uses independent retained-solid intersection, not an outline
assumption. The fabrication audit keeps panel operations outside stock packing,
includes physical dowel relationships, and preserves full precision. Optional
file fields are backward compatible. The editor audit preserves both references,
the existing supported-face policy, local/global undo, and explicit invalid
input corrections. React checks favored transient callback registration and
semantic regions rather than duplicating the whole draft in project state.

All prior R1–R15, scoped A–D, Q1–Q11, closure A–J tests remain in the complete
renderer/Electron gate. No prior regression was removed or weakened. This is
an adjacent self-audit, not another independent review.

## Final gates and scope

| Gate                                                 | Result / evidence                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Full desktop `npm test --workspace=@carvd/desktop`   | Exit 0: **4,461 renderer / 184 files**, **213 main / 9 files**, **141/141 Electron in 2.4m**. `full-desktop-final2.log`. This includes all original 135 Electron tests unchanged plus six new route tests.                                                                                                                                      |
| Focused round-6 regressions                          | **107/107 across four files**, `focused-final-green.log`; three additional file-hook regressions pass within the full suite. Net new renderer tests: **110**.                                                                                                                                                                                   |
| Desktop lint / typecheck                             | Exit 0, `lint.log`, `typecheck.log`.                                                                                                                                                                                                                                                                                                            |
| Configured Prettier / changed docs and Electron spec | Exit 0, `format.log` and explicit changed-file checks.                                                                                                                                                                                                                                                                                          |
| Production build / analytics boundary                | Exit 0 after all Electron tests, `production-boundary.log`: fresh production build excludes analytics E2E controls.                                                                                                                                                                                                                             |
| Actual PDF                                           | **9/9** real-jsPDF fabrication tests; six PDFs generated. Visually inspected page 1 of each Cut List and page 2 of each Project Report (glue-up, precise imperial, precise metric). Exact full strings and >=4pt metric column separation are asserted. `k8-layout-green.log`, `pdf/`.                                                          |
| Diff / scope / security                              | `git diff --check` clean. No unexpected deletion/mode change, package/version/lockfile/website/distribution diff. `scope-security.json`: zero common private-key/GitHub/Stripe/AWS/OpenAI secret signatures in current touched sources and whole-branch added lines; zero focused/skipped tests, debugger statements, or temporary diagnostics. |

All requested K1–K8 findings are fixed; known open P0/P1: **0**, deferred: **none**.

No release, merge, push, PR, version bump, dependency/lockfile update, packaging,
signing, notarization, website or distribution change is authorized or performed.

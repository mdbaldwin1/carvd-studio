{
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi; node -e \"const c=require('child_process'),v=require('./packages/website/package.json').version;let p='';try{p=JSON.parse(c.execSync('git show HEAD~1:packages/website/package.json',{encoding:'utf-8'})).version}catch{}if(v===p){console.log('Skip: website v'+v+' unchanged');process.exit(0)}console.log('Build: website v'+p+' -> v'+v);process.exit(1)\"",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
